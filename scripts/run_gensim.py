# Update data paths to use /data instead of data_dir
TRAINING_FILE = f"/data/{dataset_name}_train/tsv/training.tsv"
TEST_FILE = f"/data/{dataset_name}_test/tsv/test.tsv"
VOCAB_FILE = f"/data/{dataset_name}_train/csv2/{dataset_name}_vocab.csv" 